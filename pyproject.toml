[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mario-rl"
version = "0.1.0"
description = "PPO reinforcement learning project for gym-super-mario-bros."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
