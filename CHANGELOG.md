# Changelog

## 2.0.0 - 2026-09-10

- 将 `flag_get` 设为训练奖励和最佳 checkpoint 选择的首要完成指标，同时保留进度指标用于诊断。
- 默认不再叠加第二份手动进度奖励，并输出完整 reward components 便于审计。
- 训练入口支持配置 `n_epochs` 和通过 `--freeze-features` 冻结 CNN，默认行为保持不变。
- 评估和播放入口支持独立的策略随机 seed，可复现随机动作序列。
- 增加冻结视觉特征的最小微调脚本和对应单元测试。
- 冻结视觉特征的 seed 43 模型首次从完整起点返回 `flag_get=True`；30 局成功 1 次，并保存完整录像和动作轨迹。
- 发布包同时保留通关候选、原确定性 baseline 和前沿技能模型。

## 0.2.0 - 2026-09-09

- 增加最短和最长连续跳跃限制。
- 进度奖励只计算首次到达的新位置，并增加死亡、卡住、里程碑和跑动奖励选项。
- 增加确定性/随机双评估、成功阈值指标和独立最佳模型保存。
- 增加动作前缀恢复与完整/前沿混合课程训练。
- 允许加载模型时覆盖 `n_steps` 和 `batch_size`，控制并行训练内存占用。
- 评估和图形播放支持前沿动作配置。
- 补齐可安装包元数据、命令行入口、测试、模型卡和发布脚本。

## 0.1.0

- 初始 PPO 训练、评估、播放、checkpoint 和 TensorBoard 支持。
